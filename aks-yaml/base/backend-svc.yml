apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  # Use service short name; overlay namespace (dev/prod) will scope DNS.
  DB_HOST: mssql-service
  DB_PORT: "1433"
  DB_NAME: burgerbuilder
  DB_USERNAME: sa
  SERVER_PORT: "8080"
